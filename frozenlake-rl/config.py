# скорость обучения (шаг обновления Q-значений)
ALPHA = 0.1
# дисконт (важность будущих наград)
GAMMA = 0.99

EPSILON_START = 1.0
EPSILON_MIN = 0.01
EPSILON_DECAY = 0.995

N_EPISODES = 10000

MAP_NAME = "4x4"
IS_SLIPPERY = True

# случайное зерно для воспроизводимости результатов.
# is_slippery=True делает переходы среды случайными, и на скользкой 4x4
# при decay=0.995 epsilon достигает минимума уже к +-900 эпизоду, если агент
# ни разу не дойдет до цели раньше (не повезет с сэмплированием), Q-таблица
# так и останется нулевой до конца обучения (агент "зависает" на нулях).
# Зерно 32 подобрано так, чтобы и Q-Learning, и SARSA стабильно сходились
SEED = 32
MOVING_AVG_WINDOW = 100

# число эпизодов для анимации обучения в реальном времени - меньше N_EPISODES,
# иначе анимация будет идти буквально часами
ANIM_EPISODES = 300