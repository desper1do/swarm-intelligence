# скорость обучения (шаг обновления Q-значений)
ALPHA = 0.1
# дисконт (важность будущих наград)
GAMMA = 0.99

EPSILON_START = 1.0
EPSILON_MIN = 0.01
EPSILON_DECAY = 0.995

N_EPISODES = 10000

ENV_ID = "CliffWalking-v1"
GRID_SHAPE = (4, 12)
# в отличие от FrozenLake, здесь среда детерминированная - переходы не случайны,
# is_slippery=False всегда (у CliffWalking такой параметр тоже есть, но
# по заданию не используется)
IS_SLIPPERY = False

# случайное зерно для воспроизводимости результатов (влияет только на
# epsilon-greedy выбор действий, т.к. сама среда детерминирована)
SEED = 32
MOVING_AVG_WINDOW = 100

# число эпизодов для анимации обучения в реальном времени - меньше N_EPISODES,
# иначе анимация будет идти буквально часами
ANIM_EPISODES = 300